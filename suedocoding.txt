Functions I will need

  removeFriend = id => {
    // Filter this.state.friends for friends with an id not equal to the id being removed
    const friends = this.state.friends.filter(friend => friend.id !== id);
    // Set this.state.friends equal to the new friends array
    this.setState({ friends });
  };

This can be used in the collection process when onclicking the cards and collecting ID's

    <span onClick={() => props.removeFriend(props.id)} className="remove">
      𝘅
    </span>

    Onclick function above can be used to target the ID of the pokemon (used in the friendcard.js component file.)


Sorting an array in random order

var points = [40, 100, 1, 5, 25, 10];
points.sort(function(a, b){return 0.5 - Math.random()});

